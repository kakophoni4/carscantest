databases:
  - name: carscaner-db
    plan: free
    databaseName: carscaner
    user: carscaner_user
    ipAllowList: []

services:
  - type: web
    name: carscaner-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: carscaner-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGINS
        value: '["https://carscaner-frontend.onrender.com"]'
    healthCheckPath: /api/health

  - type: worker
    name: carscaner-worker
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    dockerCommand: python -m worker.scheduler
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: carscaner-db
          property: connectionString
      - key: SCRAPER_MAX_PAGES
        value: "15"
      - key: SCRAPER_INTERVAL_MINUTES
        value: "60"

  - type: web
    name: carscaner-frontend
    runtime: docker
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: ./frontend
    plan: free
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://carscaner-backend.onrender.com/api
